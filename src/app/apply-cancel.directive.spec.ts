import { ApplyCancelDirective } from './apply-cancel.directive';

describe('ApplyCancelDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyCancelDirective();
    expect(directive).toBeTruthy();
  });
});
